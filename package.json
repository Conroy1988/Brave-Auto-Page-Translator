{
  "name": "brave-auto-page-translator",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "A configurable automatic webpage translator for Brave and Chromium browsers.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "validate": "node scripts/validate.mjs && npm test",
    "package": "node scripts/package.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
